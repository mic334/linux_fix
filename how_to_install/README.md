# IQmol Executable Instructions

This directory contains the **IQmol executable (`iqmol.exe`)**.

---

## Usage Instructions

Before running IQmol, you must first install the **fake `qt5-default` package** from the corresponding folder in this repository. This ensures that all dependencies required for IQmol 2.15 on Ubuntu 24.04 LTS are satisfied.

Once the package is installed, you can launch IQmol:

```bash
dpgk -i ./qt5-default_5.99.99+fake-13ubuntu37_all.deb
./iqmol.exe

